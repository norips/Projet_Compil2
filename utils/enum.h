#ifndef ENUM_H
#define ENUM_H
#define Fun 800
#define L   801
typedef enum { typeCon, typeId, typeOpr } nodeEnum;
typedef enum { typeVar, typeFun, typePro, typeArr, typeNone } symEnum;
typedef enum {integer, boolean, arrInt, arrBool} typeEnum;
#endif 
